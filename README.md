# Lost & Found

A microservices-based lost and found application.

- **frontend/** - React web application
- **gateway/** - Flask API gateway
- **services/** - Backend microservices
- **Configuration**: All settings are in `.env` at the repo root
